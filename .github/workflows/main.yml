name: Build and Push to Config Repo

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ... (Add your Docker build/push steps here) ...

      - name: Update image tag in Config Repo
        uses: EndBug/add-and-commit@v9
        with:
          repository: prachishah0105-ai/todo-app-config
          branch: main
          add: 'overlays/staging/kustomization.yaml'
          # Use the secret you saved earlier
          github_token: ${{ secrets.GIT_TOKEN }} 
          message: "Update staging image to ${{ github.sha }}"
