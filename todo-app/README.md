# Todo App - Step 1

This is the web server for the course project.

## Features
- Outputs "Server started in port NNNN" on startup.
- Uses an environment variable `PORT` to define the port.

## How to run
1. Install dependencies: `npm install`
2. Start the app: `PORT=3000 npm start`
